// import Place from '../src/place'

describe('Place', () => {
  // let place

  beforeEach(() => {
    // place = new Place({}, {})
  })

  it('instantiates', () => {
    // expect(typeof place).toBe('Place')
    expect(true).toBe(true)
  })
})
